# ApexQuant AI — Open Tasks Report

Date: 2026-08-08 · Open tasks: 157

## Drafts (awaiting acceptance) — 157

| Task | Title | Brief description |
|---|---|---|
| #12 | Surface token expiry countdown on the Broker Health dashboard so the operator sees it before midnight | The backend now tracks `token_expiry_minutes` and `token_expiry_warning` in every BrokerHealth snapshot, but the dashboard has no UI to show them. An operator won't know the token is about to expire unless they happen to |
| #13 | Start the expiry monitor automatically when the adapter goes live so it can't be forgotten | `TokenExpiryMonitor` exists and is fully tested, but nothing starts it. If the caller forgets to call `monitor.start()`, the proactive warning never fires — the system silently falls back to reactive detection only. |
| #14 | Prevent paper-fallback orders from being mistaken for live fills in post-session reconciliation | When the token expires mid-session and orders are rerouted to PaperBroker, those paper fills land in the same order table as real fills. The end-of-day reconciliation engine currently has no way to tell them apart, which |
| #15 | Wire portfolio pre-check into the signal flow so exposure and capital limits are enforced before RC-8 sees an order | RC-10C1 delivered the Portfolio Core engine but it is not yet wired into the live signal path. The spec requires: |
| #24 | Show equity curve and P&L history on the Portfolio page so operators can see performance over time | The Portfolio panel currently shows only the current snapshot — a single moment in time. Operators have no way to see whether equity has been rising or falling, when drawdown started, or what today's intraday P&L trend l |
| #25 | Prevent stale position data on the Portfolio page when the API server restarts | The portfolio snapshot endpoint (`GET /api/portfolio/snapshot`) currently derives open-position data from the legacy paper-trader JSON state. If the paper-trader state file is absent or the server restarts mid-session, t |
| #30 | Keep the sidebar badge accurate when discrepancies are resolved from any page | The reconciliation badge uses its own independent query key ("reconciliation-badge") with a 2-minute poll. When an operator resolves a discrepancy from the Automation Health page, the badge query is not invalidated, so i |
| #31 | Confirm the reconciliation badge stays hidden when there are no open issues | The badge logic filters open_discrepancies by requires_manual_review. A unit test verifying the hook returns 0 for an empty list, a list with no review-required items, and a list with mixed items would prevent regression |
| #36 | Run the reconciliation probe automatically so operators don't have to call it manually | The `check_reconciliation_probe()` function and `GET /api/phase20/reconciliation/probe` endpoint exist, but the probe only fires if something calls it. Without a scheduled trigger, a missed reconciliation night still won |
| #37 | Show missed-reconciliation alerts on the Broker Execution page so operators see them in context | When the probe fires a `RECONCILIATION_MISSED` notification it lands in the generic notification feed, but operators checking the Broker Execution dashboard have no in-context banner to know today's reconciliation was sk |
| #40 | Catch sector-level exposure warnings disappearing silently when positions close | The current exposure warning smoke tests cover instrument-level warnings (single stock). Sector warnings follow the same code path but are computed over aggregated sector buckets, so a regression in the sector rollup cou |
| #54 | Confirm the resolved section always shows the note from the most recent resolution, not a stale one | The UI renders `resolved_note` from the API response, but there's no test confirming that after a resolve → reopen → resolve cycle the GET endpoint returns the second note (not the first). A stale cached note on the dash |
| #55 | Prevent a discrepancy from being resolved while a reopen is still in flight | The dashboard allows simultaneous resolve and reopen mutations on the same discrepancy row if both buttons are clicked quickly. Because the Python handler is spawned per-request, a late-arriving resolve after a reopen ca |
| #56 | Make it clear to operators why the Reopen button is missing on old discrepancies | After hiding the Reopen button for discrepancies resolved more than 48 hours ago, operators may be confused about why the button is gone for some items but not others. A tooltip or a small "Too old to reopen" label would |
| #57 | Confirm the Reopen cutoff is enforced on the server so a direct API call can't bypass it | The 48-hour gate is currently only enforced in the UI. A determined operator (or a script) could POST directly to `/broker/reconciliation/reopen` and reopen a discrepancy closed months ago. The API server should reject r |
| #58 | Confirm the badge disappears in the browser when positions close, not just in unit tests | The existing test suite (including the new transition tests) uses pure logic and static source analysis. A stale-render bug — where React's reconciler fails to remove a DOM node even though the warnings array is empty —  |
| #59 | Make sure the exposure badge updates immediately when the API returns fresh data, not after a full page reload | The portfolio snapshot is polled every 15 seconds (REFRESH_INTERVAL). If all positions close between polls, the badge stays visible for up to 15 seconds showing a stale count. Reducing the interval when warnings are acti |
| #60 | Confirm the badge disappears in the browser when positions close, not just in unit tests | The exposure warning badge is gated on `exposureWarnings.length > 0`, which is proven correct by unit tests. But there's no end-to-end or browser-level check that confirms the badge actually unmounts from the DOM when th |
| #61 | Make the exposure badge pulse or animate when a new CRITICAL breach appears so operators notice immediately | The badge now correctly turns red for CRITICAL breaches, but a static colour change can be missed when the operator is looking elsewhere on the page. A brief pulse or ring animation when the badge first escalates to red  |
| #68 | Make sure limit edits take effect in running strategies without waiting for the next order | Session overrides are stored in the Node.js API server process and returned on GET /api/portfolio/config, but the Python strategy/execution layer reads PortfolioConfig directly from environment variables at import time.  |
| #69 | Confirm overrides survive a hot-reload of the API server so operators don't lose mid-session settings unexpectedly | Session overrides are intentionally in-memory and reset on restart. But ts-node/nodemon hot-reloads during development also flush them, which can surprise operators who didn't trigger a full restart. The current behavior |
| #98 | Confirm the UNREACHABLE badge appears in the browser when the health API goes down, not just in source analysis | There is a branch in PortfolioLive.tsx that sets `overallStatus = "UNREACHABLE"` when `healthQuery.isError && !health`. The existing source-analysis test asserts the pattern is present in the source, but no mounted-compo |
| #99 | Catch live position-row updates silently breaking if the snapshot polling config changes | The `snapshotQuery` in PortfolioLive.tsx polls at `REFRESH_INTERVAL` just like `healthQuery`, but there are no mounted-component tests confirming that a new open position returned by the server actually appears in the po |
| #100 | Redeploy to production so the yfinance fix reaches the live environment | The production/deployed instance still fails every scan tick with |
| #106 | Confirm the consolidated outage banner disappears in the browser when the API recovers, not just in unit tests | Task 90 added a consolidated "API server unreachable" banner that appears when both snapshotQuery and healthQuery fail simultaneously. The suppression logic and recovery path are covered by static source-analysis tests,  |
| #107 | Prevent operators from seeing a stale snapshot for up to 15 seconds after the API server restarts | When the API server restarts, React Query may serve the previous cached snapshot for up to REFRESH_INTERVAL / 2 (7.5 s) before issuing a real network request. During that window the portfolio page shows stale equity and  |
| #108 | Confirm the config panel refreshes in the browser the moment an operator saves a limit, not after the next poll | Task 91 adds a source-analysis test confirming the mutation's onSuccess handler calls queryClient.invalidateQueries. But there is no browser-level (end-to-end or integration) test that actually renders the component, fir |
| #109 | Make sure limit edits survive a hot-reload of the API server so operators don't lose mid-session overrides unexpectedly | When an operator patches a limit via the config panel, the override lives in server-side session memory. If the API server hot-reloads (e.g. after a code push), those overrides are silently lost and the panel snaps back  |
| #112 | Fix pre-existing TypeScript errors in dashboard pages so tsc can catch real regressions | Running `pnpm --filter trading-dashboard exec tsc --noEmit` produces ~25 pre-existing errors across 7 files. All are untyped `apiJson` return values (`unknown`) used without type-narrowing. They prevent `tsc --noEmit` fr |
| #113 | Confirm CORS blocks non-Replit origins so a misconfiguration can't open the API to the public | Phase 1A replaced the wildcard `app.use(cors())` with an explicit allowlist in `artifacts/api-server/src/app.ts`. The logic is correct by inspection but has zero automated test coverage. A future refactor could silently  |
| #114 | Set production API URLs as environment variables so deployed connectivity is explicit, not inferred | Phase 1A added env-var support and production localhost guards for both the dashboard (`VITE_API_BASE_URL`, `VITE_WS_BASE_URL`) and mobile (`EXPO_PUBLIC_API_BASE_URL`, `EXPO_PUBLIC_WS_BASE_URL`). In dev both fall back to |
| #122 | Confirm the MARKET CLOSED badge disappears in the browser when the market reopens on Monday morning | The existing E2E test (market-closed-badge.spec.ts) proves MARKET_CLOSED renders during a weekend. There is no corresponding browser-level test that the badge transitions away from MARKET_CLOSED to LIVE (or DELAYED) when |
| #123 | Catch a stale MARKET CLOSED badge persisting across pages when the market reopens | DataFreshnessBar is rendered on every data-driven page in the dashboard (enforced by freshness-coverage.test.ts). The existing E2E test only checks the Portfolio page. If a page uses a different React Query refetch inter |
| #127 | Prevent risk limits from silently reverting to hardcoded defaults when the config file fails to load | Phase 2B found that `portfolio/config` returns `{loaded: false, error: "No module named 'pydantic'"}`. |
| #128 | Confirm scanner automatically recovers to 50/50 symbols on Monday market open after a weekend data gap | Phase 2B and 2C found that the scanner consistently shows 48/50 symbols (LTIM and TATAMOTORS |
| #129 | Make sure a partial position exit immediately updates the sector exposure badge, not just the full-close | Phase 2D S6 confirmed that partial exits correctly reduce position quantity in the in-memory |
| #166 | Warn operators when a stock's strategy has a poor track record in today's regime | The Strategy×Regime advisory panel on Trade Decisions shows the *best* strategy for the current regime at the page level. But individual stock cards don't reflect whether their assigned strategy historically underperform |
| #167 | Persist the strategy×regime advisory panel across page refreshes so the panel is always visible, not just on first load | The StrategyRegimeAdvisoryPanel fetches /optimisation/strategies on every cold page load with a 3-minute stale time. If the API server is briefly slow (e.g. on cold start), the panel silently disappears for the whole 3 m |
| #168 | Confirm the performance cache clears itself when a new paper trade is recorded, not just after 30 seconds | The TTL cache added to performance_engine.py refreshes every 30 seconds. When an operator places a paper trade, the portfolio page will show stale P&L figures for up to 30 seconds. Calling `_clear_perf_cache()` from the  |
| #169 | Confirm performance analytics still pass at 1,000 trades without the 100 ms guarantee degrading | The benchmark added in Task |
| #170 | Confirm the rolling 30-day accuracy chart still updates correctly in the browser after the sliding-window refactor | The _rolling_30d() function in learning_analysis.py was refactored from an O(n×d) inner loop to a sorted two-pointer sliding window. The algorithm produces identical output, but the change is deep enough that a visual /  |
| #171 | Warn operators on the Trade Decisions page when AI accuracy has been declining for the past 30 days | The rolling 30-day accuracy trend (Improving / Stable / Declining) is computed in Phase 5D.4 but is not surfaced where operators make decisions. An operator placing a trade while the AI is in a sustained Declining trend  |
| #172 | Prevent the AI health score from silently returning stale data when the learning analysis is recomputed twice per request | _compute_health_score() in ai_performance/shared_services.py was discovered to call compute_learning_analysis() a second time (redundantly) on every request. The fix passes the already-computed result in. The same patter |
| #178 | Keep the runtime-managed key exclusion list in one place so it can't drift between modules | The `_RUNTIME_MANAGED_KEYS` frozenset (PGPASSWORD, PGUSER, PGHOST, PGPORT, PGDATABASE, DATABASE_URL, REPLIT_DOMAINS, REPL_ID, REPLIT_DB_URL) currently lives only in `security_checker.py`. If a future check in `config_che |
| #179 | Confirm accuracy session history shows on the Pre-Open Intelligence page after multiple trading days | The `/api/preopen/accuracy/history` endpoint returns up to 5 previous sessions with grade, MAE %, hit rate, and confirmation rate. Operators currently only see today's session. A trend chart would let them see whether pr |
| #180 | Prevent the 09:20 reconciliation from running when actual prices are all null and producing a misleading empty accuracy report | `_phase_09_20_reconcile()` in `preopen_scheduler.py` calls `reconcile_session()` even when no live quotes are available (outside market hours, or when the quote service is down). This inserts 10 rows with `actual_open=nu |
| #181 | Confirm the accuracy card refreshes automatically after market open without needing a page reload | The accuracy `useQuery` in `PreOpenIntelligence.tsx` polls every 60 seconds. But the reconciliation data first appears around 09:20 IST, which means the card will show "Check back after the session" for hours and only fl |
| #182 | Confirm the readiness score also updates when auto-paper entries open, not only when positions close | Task |
| #183 | Show a live 'Last updated' timestamp on the Data Quality card so operators know the score is current | After the readiness score auto-invalidates on a paper trade, there is no visible signal that the score just refreshed. Operators have no way to confirm the number they see is fresh vs stale. A small "Updated X seconds ag |
| #184 | Prevent the exits/tick endpoint from publishing a readiness event when no positions exist to evaluate | The current `paper.trade.recorded` publish condition is `closedCount > 0 OR success === true`. Because exits/tick always returns `success: true` even when there are zero open positions to evaluate, the Live Readiness pag |
| #202 | Confirm the VIX risk label on the Executive Dashboard tile changes colour when VIX crosses HIGH and EXTREME thresholds | The macro tile already receives `vix_risk_level` from `get_macro_intelligence_snapshot()`. Operators need an at-a-glance visual signal — not just text — when VIX is dangerous. A colour-coded badge (green LOW, yellow MEDI |
| #203 | Prevent the macro score from showing stale data when VIX fetch fails and the cache has expired | `get_volatility_intelligence()` falls back to `_vix_from_regime()` (returning ~18.0) when yfinance fails. If this happens during a real VIX spike, the Executive Dashboard macro tile will display MEDIUM risk instead of HI |
| #204 | Make sure the Macro Intelligence tile never shows an error state on the Executive Dashboard when one sub-module is slow | `get_macro_intelligence_snapshot()` has a top-level try/except that returns safe defaults, but the individual `_load_*_safe()` helpers can each take up to ~3 s (yfinance). If the Executive Dashboard times out on the API  |
| #205 | Confirm PRE-OPEN ADVISORY hints appear when STRONG_GAP_UP data exists, and stay hidden when none qualify | The new pre-open advisory panel in Trade Decisions hides itself when there are no qualifying hints, which is the correct behavior outside of pre-open hours. But there are no automated tests confirming the panel renders c |
| #206 | Let operators dismiss individual PRE-OPEN ADVISORY hints they've already reviewed | The PRE-OPEN ADVISORY panel currently refreshes every 60 seconds and re-shows all qualifying hints. Once an operator has reviewed a hint and decided to act or pass, the card keeps reappearing, adding noise during the ses |
| #207 | Track whether PRE-OPEN ADVISORY candidates confirmed as gap-ups after market open, to measure prediction accuracy | The pre-open engine already has a reconciliation module (`preopen_accuracy.py`) that compares indicative prices to actual opens. But it doesn't yet track specifically which symbols crossed the signal-hints threshold (STR |
| #208 | Confirm the Performance Snapshot shows accurate stats after paper trades, not zeros on a fresh portfolio | The Performance Snapshot section on the Portfolio page fetches /api/performance/summary and /api/performance/equity. With a fresh portfolio (no trades), everything shows zeros or dashes — which is correct, but there are  |
| #209 | Make the equity sparkline on the Portfolio page visible immediately by seeding it from the portfolio snapshot's equity history | The 14-day equity sparkline on the Performance Snapshot section shows "Collecting equity history…" until the equity endpoint returns at least 2 data points. Because equity points are only written when paper trades close, |
| #210 | Add a P&L trend badge to the Portfolio sidebar entry so operators see session performance at a glance without opening the page | The Performance Snapshot is only visible when operators navigate to the Portfolio page. A small today-P&L badge on the "Portfolio" sidebar nav item (positive = green, negative = red) would give operators instant context  |
| #211 | Confirm Strategy Intelligence and AI Performance stay accurate after 30+ real paper trades, not just on an empty dataset | Both dashboards are now enabled and loading, but the dataset is empty (0 closed trades). The analytics logic — regime breakdowns, calibration curves, confidence bucketing, and health scores — has only been tested in unit |
| #214 | Confirm Strategy Optimisation gives sensible results after 30+ real paper trades, not just in unit tests | The integration smoke test added in |
| #215 | Confirm Strategy Optimisation and AI Performance stay accurate after 30+ real paper trades, not just on an empty dataset | The integration test for Strategy Optimisation ( |
| #221 | Confirm the pre-open accuracy report shows real gap-prediction data after a morning cycle completes | The Pre-Open Intelligence module completed its first full cycle today |
| #222 | Pre-warm the Research Lab snapshot cache during the post-scan pipeline so the cold yfinance fetch never blocks an operator's HTTP request | Task |
| #227 | Confirm Paper Analytics tabs still show accurate data after 30+ real paper trades, not just test fixtures | Phase 8.2 tests use mocked upstream data. After the system accumulates real paper trades from the auto-paper module, all 12 dashboard tabs should show non-zero, coherent figures. An integration smoke test that runs the f |
| #229 | Mark each trade entry and exit on the equity curve so operators can see which trades drove drawdowns | The equity curve chart in the Trades tab shows portfolio value over time with drawdown shading, but operators cannot tell which trades caused dips or peaks. Adding dots at each trade exit (and optionally entry) timestamp |
| #230 | Confirm the equity chart still renders correctly when the paper portfolio has exactly 1 or 2 data points | The EquityWithDrawdownChart component shows an empty state when fewer than 2 points exist. With exactly 2 points (one trade), the chart renders but the drawdown shading polygons need at least 2 index positions. A portfol |
| #231 | Confirm the AI Health score updates on the Executive Dashboard within 60 seconds after a new scan completes | The Executive Dashboard fetches /api/ai/summary every 60 seconds. If a fresh scan produces new signal outcomes, operators need to see the updated AI Health Score within one poll cycle — not after a page reload. |
| #232 | Show the AI Health score breakdown (accuracy, calibration, consistency) on hover so operators understand what drives the score | The AIHealthTile on the Executive Dashboard shows a composite score (0–100) and label, but operators don't know which sub-component (prediction accuracy, calibration quality, consistency, etc.) is pulling the score down. |
| #233 | Confirm the regime badges appear correctly after 5+ paper trades exist, not just on an empty dataset | The NO TRADES / LOW SAMPLE / SUFFICIENT DATA badges in the Regimes tab are driven by `r.trades` from the strategy/regimes endpoint and per-strategy `regime_breakdown`. With zero paper trades recorded the table is empty a |
| #234 | Show which strategies are viable for the current regime on the Trade Decisions page so operators choose the right one | The Regimes tab now shows per-strategy coverage for the live regime (NO TRADES / LOW SAMPLE / SUFFICIENT DATA). But operators choosing a strategy on the Trade Decisions page don't have this visibility — they may select a |
| #235 | Prevent stale regime data from silently masking a regime transition that happened mid-session | The Regimes tab polls phase13/regime every 60 seconds. If the market transitions from TRENDING to RANGE_BOUND during a session, the highlight jumps to the new row — but operators may not notice the row switched. A brief  |
| #236 | Apply _as_str coercion to string KPI fields in the shared_services snapshot functions used by non-executive pages | The executive widgets.py now guards every string KPI field with _as_str(). However, shared_services.py exposes flat snapshot dicts (get_executive_snapshot, get_executive_summary) that are consumed by non-executive pages  |
| #237 | Confirm the Executive Dashboard summary endpoint still returns valid JSON when every upstream module times out simultaneously | Each widget has _as_str() guards against bad data shapes, but there is no test confirming the /api/executive/summary endpoint returns a well-formed JSON response (not a 500) when all upstream loaders raise exceptions sim |
| #238 | Show pre-open gap-down advisory hints alongside gap-ups so operators can act on both directions | The Pre-Open Advisory panel on Trade Decisions currently surfaces only STRONG_GAP_UP candidates (opportunity_score ≥ 70). Strong gap-down symbols are equally important — an operator might want to avoid holding a gapping- |
| #239 | Confirm the Pre-Open Advisory panel hides correctly outside pre-open hours, not just when hints are empty | The PreOpenAdvisoryPanel hides when hints.length === 0, which correctly handles post-09:20 once snapshots go stale. But there is no explicit time-window guard in the frontend or backend — if stale snapshots survive into  |
| #240 | Confirm the Research Lab snapshot shows the correct grade after a live scan, not a stale cached value | `research_lab/shared_services.py` caches the snapshot for 5 minutes to avoid expensive yfinance calls on every request. If a scan completes during that window, the cached grade/trend fields continue to reflect pre-scan d |
| #241 | Prevent the Portfolio Performance snapshot from silently showing a wrong grade when the performance engine is unavailable | `portfolio_performance/shared_services.py` calls `api.get_summary()` and falls back to `disabled_response()` if the status is not ENABLED. However if the engine raises an exception (DB unreachable, zero-division, etc.) t |
| #244 | Confirm the Paper Analytics tile in the Executive Dashboard still shows correctly after the API server restarts mid-session | The Paper Analytics tile reads from the executive/summary endpoint, which calls _load_paper_analytics() on every request. If the API server restarts mid-session, the executive/summary query in the browser retries automat |
| #329 | Confirm load_all stays fault-tolerant if any other section loader raises at startup | _load_paper_analytics() catches exceptions internally and returns a safe error dict. |
| #330 | Confirm the Executive Score neutral fallback survives a hot-reload without resetting to zero mid-session | The neutral 50.0 fallback for paper_analytics is computed at request time from |
| #334 | Stop repeated Opportunity Scan polls from spawning Python every time, not just concurrent ones | Task |
| #335 | Confirm all waiting callers see the failure when an in-flight Opportunity Scan errors mid-request | The coalescing guard means multiple callers share one Promise. If that Promise rejects (Python crashes or times out mid-scan), every co-waiter should receive the error rather than hanging indefinitely. This is not curren |
| #336 | Show the oldest-to-newest domain score trend so operators can see if a specific domain is getting worse over time, not just the latest value | The per-domain mini-sparklines now show trends visually, but operators have no quick signal for which domain is degrading fastest. An auto-highlighted "most degraded" badge (e.g. red border + label "⚠ AI −12pts") on the  |
| #337 | Confirm the domain sparkline grid still looks correct when a run is missing scores for one or more domains | The `domain_scores` JSONB field can legitimately be missing keys if a domain validator was disabled during a run. The current DomainMiniSparkline renders "—" for null scores, but there are no tests covering this. A run w |
| #338 | Let operators export per-domain trend data as CSV so they can track quality degradation in their own tools | The Export tab currently exports a full JSON report or an issues-only CSV. Now that per-domain history is visible in the History tab, operators reviewing quality trends over time will want to pull the domain_scores time- |
| #339 | Stop the Agent Operations stats bar from showing 0 agents for 25 seconds after page load | The Agent Operations page OverallHealthCard and stats strip briefly show "Unknown 0/100" and "Agents: 0, Active: 0" for approximately 25 seconds after page load. This happens because: |
| #340 | Make the diagnostics panel show the real active agent count, not zero | The Backend Diagnostics panel on Agent Operations reads `active_agents_from_snapshot` from the `ops_active_agents` KV key, but `get_ops_centre_agents()` never writes that key. As a result the panel always shows "—" for A |
| #341 | Confirm all four dashboard pages show the same agent count after an API server restart, not just when manually tested | Cross-page consistency (Task |
| #342 | Confirm the Executive Score drops visibly when a live data source fails during a session | Data Quality is now wired into the Executive Score at 10% weight. There are currently no end-to-end integration tests that verify the composite score actually drops when `get_data_quality_snapshot()` returns a degraded s |
| #343 | Show the Data Quality grade on the Executive Score ring tooltip so operators know which component dropped the score | Data Quality now contributes 10% of the Executive Score. When the score drops, operators have no quick way to tell whether the fall came from data quality, AI health, or another component. Adding the Data Quality grade ( |
| #344 | Show the Data Quality grade on the Executive Score ring tooltip so operators know which component dropped the score | The Executive Score ring shows a composite number but the tooltip only shows component weights. When data quality drops, operators have no quick way to see the grade (A/B/C/D) without navigating away. Surfacing the grade |
| #345 | Confirm the trend chip stays Stable — not Declining — when data quality runs don't exist yet | The new trend logic in get_data_quality_snapshot() falls back to "Stable" when fewer than 2 history runs exist. There are no tests confirming this edge-case — if the fallback ever breaks, the UI would show a misleading D |
| #346 | Prevent stale regime data from silently masking a regime transition that happened mid-session | The market regime displayed on multiple pages derives from the phase7 snapshot. If a regime transition happens mid-session and the snapshot is not refreshed, all downstream pages (Executive Dashboard, Trade Decisions, et |
| #347 | Confirm the AI Health tile also recovers gracefully when the API server restarts mid-session | The Executive Dashboard has a dedicated AI Health tile that fetches from `/api/ai/summary` on a 60-second poll (queryKey: `ai-summary-exec`). There are currently no tests for the restart-recovery path on this tile — only |
| #348 | Confirm the Research Lab tile shows fresh data after a server restart, not a stale grade | The Executive Dashboard's Research Lab tile polls `/api/research-lab/snapshot` (queryKey `research-lab-exec`) on a 60-second interval. There are no tests for the error→recovery transition on this tile. If the server rest |
| #349 | Show operators exactly how long to wait when the Scan button is rate-limited on mobile | When `POST /api/live-data/scan/run` returns `{ status: "RATE_LIMITED", retry_in_s: N }`, the mobile Signals tab currently shows the generic "Scan failed. Check system health and try again." message. Operators have no ide |
| #350 | Confirm the mobile Scan button stops the spinner and surfaces an error when the server is busy | The mobile Scan button now uses a fire-and-forget flow: it kicks off the Phase 7 scan and then polls `/live-data/scan/status` to detect completion. The rate-limit (RATE_LIMITED) and network-error branches both need to cl |
| #351 | Confirm the sparkline stays accurate when two positions share the same price in consecutive scans | buildSparkPoints de-duplicates consecutive identical prices to avoid flat artefacts. If a stock's price genuinely doesn't move for several scans, the dedup logic could collapse a long holding period down to just 2 points |
| #352 | Prevent the sparkline from breaking when a timeline event carries a price of zero or null | buildSparkPoints filters events with `e.price > 0`, which guards against zeros. However there is no test that verifies a zero-price or null-price event is silently excluded rather than crashing or contributing a 0-point  |
| #353 | Trigger a price snapshot automatically after every scan so sparklines fill in without manual API calls | The `POST /phase11/price-snapshots/record` endpoint now exists, but nothing calls it automatically. Sparklines only get richer if the snapshot is recorded right after each scan completes. Without this wiring, operators m |
| #354 | Confirm sparkline snapshots from yesterday don't bleed into today's chart | `get_price_history()` filters by `DATE(recorded_at AT TIME ZONE 'Asia/Kolkata') = today_ist`. If the filter has an off-by-one error near midnight IST, stale data from a prior session would corrupt today's sparkline shape |
| #355 | Prune old intraday snapshots so the price-history table doesn't grow indefinitely | `phase11_price_snapshots` accumulates one row per open symbol per scan, every ~30 min during market hours. Without pruning, the table grows by hundreds of rows per day. After a month of paper trading this adds noticeable |
| #356 | Confirm all three E2E specs pass together in CI without timeouts | The port-collision fix sets reuseExistingServer: true, but the Vite server startup time (timeout: 60_000) has never been stress-tested with all three spec files running back-to-back in a single `pnpm run test:e2e` comman |
| #357 | Confirm the Risk Agent card recovers automatically when phase20 data arrives after a cold start | Task 313 confirmed that the card shows ERROR when no scan has run and ACTIVE once phase20 data exists. However, the Operations Centre polls its aggregator on demand — there is no integration test that drives a full cold- |
| #358 | Prevent the Risk Agent card from going dark when the SnapshotBus restarts but phase20 data is intact | get_risk_snapshot() has a three-level fallback: bus → agent → phase20. After an API server hot-reload the SnapshotBus singleton is re-created (empty), and the RiskAgent singleton is also reset. If the agent's execute_tas |
| #359 | Make sure ops-centre overview never blocks on the Risk Agent when it is slow to initialise | ops_centre.py wraps each agent collector in a 6-second per-agent timeout using ThreadPoolExecutor. The Risk Agent's execute_task() can import heavy dependencies (portfolio_store, market intelligence hub) on first call, p |
| #360 | Confirm the gate breakdown table shows correctly when all candidates pass (zero rejections) | The gate breakdown table on the Risk Agent card is only rendered when `gate_breakdown` is non-empty. When every candidate passes all gates (blocked=0 for all), the breakdown list will be empty and the table should be hid |
| #361 | Show which specific gate blocked each candidate symbol so operators can act on individual stocks | The gate breakdown table now shows how many candidates each gate blocked in aggregate (e.g. "Sector Cap → 3"). Operators still cannot see *which symbols* were blocked by each gate. Surfacing symbol-level detail (e.g. a c |
| #362 | Apply the same offline caching to the Signals tab so operators see last-known signals instead of a blank screen when offline | The Signals tab (`artifacts/trading-mobile/app/(tabs)/signals.tsx`) fetches scan signals from the API. Like the Pipeline tab before this fix, it shows a spinner with no data when the server is slow or offline. Other mobi |
| #422 | Show a run-freshness warning banner on the Missed Opps tab when the newest run is more than 7 days old | The Run ID column now lets operators spot which run each row came from, but if the newest run is weeks old, all rows are still "stale" without any explicit warning. An inline banner telling operators "newest data is N da |
| #423 | Let operators filter the Missed Opps table by run so they can compare what changed between two backtests | Now that each row shows its run_id, operators naturally want to view "only the misses from run X" to compare two runs side by side. Currently all runs' misses are merged into one list. |
| #424 | Confirm the Run ID chip on Missed Opps correctly pre-selects that run in Trade Simulation, not just navigates to the tab | Clicking a run_id chip calls `onNavigateToRun(2, runId)` which sets `latestRunId` in the parent and switches to tab 2. But `TradeSimulationTab` only uses `latestRunId` as its default if `selectedRunId` is empty — if an o |
| #425 | Run the codegen idempotency check automatically when the orval config changes so regressions can't be merged silently | `pnpm run codegen:check` now exists and works locally, but it only runs when someone remembers to call it. The highest-risk change that could reintroduce the duplicate-export bug is an edit to `lib/api-spec/orval.config. |
| #426 | Make codegen:check faster by skipping the typecheck step so it can realistically run on every save | `codegen:orval` (orval + fix-zod-index.js) runs twice during `codegen:check`. Each orval run regenerates all 319 generated files from the OpenAPI spec, which takes several seconds. If this is too slow to run routinely, d |
| #427 | Confirm a corrupt or outdated Pipeline cache is cleared from storage so it never blocks fresh data | `readSnapshot` in `lib/offlineCache.ts` removes invalid records from AsyncStorage (via `removeItem`) when decodeSnapshot returns `ok: false`. This removal path — which prevents a corrupt cache from permanently blocking t |
| #428 | Prevent the operator seeing two conflicting freshness labels at the same time on the Pipeline tab | The Pipeline tab currently has two independent freshness indicators that derive from different sources and can disagree: |
| #429 | Confirm the 'Live' badge recovers automatically once the snapshot endpoint comes back up after an error | Task 367 confirms the badge reverts to "Cached snapshot" when the snapshot errors. The opposite recovery path — snapshot was erroring, then the endpoint recovers and returns a successful response — is untested. A regress |
| #430 | Prevent the platform badge from briefly flashing 'Live' on page load when only the cached snapshot is available | The `effectivePlatform` merge logic in AIOperationsCentrePage resolves to the fast platform response (fast:true → "Cached snapshot") while the slow snapshot query is in-flight. However, if React Query's query cache alrea |
| #431 | Confirm the 'Live' badge age ticker also advances when the snapshot is freshly computed | Task 368 added a Playwright test proving the "Cached snapshot" badge age ticker advances in real time. The same setInterval / useEffect logic also drives the emerald "Live" badge (fast=false path), but that variant has n |
| #432 | Confirm staleness badges stay accurate when the browser tab is hidden for several minutes then brought back | The StalenessTag component uses `setInterval` to tick every second. Browsers throttle timers in hidden tabs, which can cause the age counter to lag behind reality when the user returns to the tab. This should be verified |
| #433 | Show a staleness badge on the AI Paper Trader page so operators know how fresh the portfolio snapshot is | Task 369 added StalenessTag to every section on the Command Centre. The AI Paper Trader is the primary screen for most operators and carries its own Phase 11 snapshot with a `generated_at` timestamp, but currently has no |
| #434 | Prevent the StalenessTag from flickering between Live and Cached on the first render before the timestamp arrives | When a section's data is loading, `generatedAt` is undefined so StalenessTag renders nothing. Once the data arrives, it immediately renders as "Live · 0s ago" and then starts counting. If the initial fetch is slow, the b |
| #435 | Confirm the AIOperationsCentrePage staleness badge also goes amber after 60 s of idle | `AIOperationsCentrePage.tsx` has its own inline staleness badge in `PlatformStatusBar` (lines 219–314) that uses an identical 1-second `setInterval` + `ageSecs > 60` pattern, but it is not exported and has no render test |
| #436 | Prevent the Live pill from showing stale seconds counts when the browser tab sleeps | Both `StalenessTag` (CommandCenter.tsx) and the inline badge in AIOperationsCentrePage.tsx rely on `setInterval(tick, 1_000)`. Browsers throttle timers in hidden tabs to ~1 tick/minute, so the displayed "Ns ago" counter  |
| #437 | Confirm the recovery push re-arms correctly after a second degradation following recovery | The current recovery tests cover a single degrade→recover cycle. A more complex but realistic path — degrade → recover → degrade again → recover again — is not covered. If the degraded-token tracking doesn't re-arm after |
| #438 | Confirm health alerts reach a device that subscribes mid-session after a degradation has already fired | `degradedSubscriberTokens` is populated as alerts are sent. A new subscriber whose token is inserted after the first degradation snapshot was processed will never appear in the set — so they'll never receive a recovery p |
| #439 | Confirm the recovery push still fires correctly after an API server restart mid-incident | The DB-persistence change was unit-tested but the end-to-end restart scenario (degrade → restart → recovery snapshot → push delivered) is not covered. A test that simulates the restart by resetting the in-memory state wh |
| #440 | Prevent orphaned degraded-token records from accumulating when a device unregisters | When Expo reports a token as `DeviceNotRegistered`, the subscription row is deleted from `push_subscriptions` but the token can still be present in `signals_cache` key `ops_health_state` indefinitely. Over time this caus |
| #441 | Confirm recovery push fires for the right subscribers when each device has a different minHealthPct threshold | The per-subscriber threshold logic was tested for alert dispatch, but there are no tests covering the recovery path when subscribers have different `minHealthPct` values. A device with 90% threshold should receive a reco |
| #442 | Confirm minHealthPct is honoured when a push registration is updated via the API, not just at insert time | The `minHealthPct` column is set at registration time, but there is no route that lets a device update its own threshold after initial registration. If an operator raises their device's threshold from 70% to 90%, they cu |
| #443 | Sync the confidence threshold from the server on launch, not just the health threshold | Task |
| #444 | Confirm the Alerts screen shows the server threshold when it opens before the launch sync finishes | registerOnLaunch() syncs minHealthPct to AsyncStorage, but PushAlertsCard reads AsyncStorage once on mount. If the Alerts tab is opened before registerOnLaunch() completes (e.g. the user taps alerts immediately on launch |
| #445 | Confirm health alerts fire at exactly the clamped threshold in a live integration test | The clamp logic (minHealthPct to [50, 90]) is now in dispatchHealthAlertPushNotifications(), but there are no integration-level tests that verify a subscriber with a stored threshold of 20 or 95 gets treated as 50 or 90  |
| #446 | Prevent the minConfidence threshold from being silently mis-applied the same way if it also reaches the DB as NULL | dispatchSignalPushNotifications() reads minConfidence from the DB with only a `?? 70` fallback and no clamp or NaN guard. If the column were ever NULL or non-finite (e.g. from a migration gap or raw insert), signals woul |
| #452 | Show operators how many filter conditions failed on each AVOID row so they can distinguish a gate-blocked setup from a genuinely weak one | When the high-confidence gate forces AVOID (two or more simultaneous filter failures at fc>=85), the table shows the OVERRIDDEN-BY-GATE badge but only displays the first filter reason in the reason column. Operators cann |
| #453 | Let operators adjust the filter-failure threshold that turns AVOID into WATCH from the settings panel without a code deploy | HIGH_CONF_AVOID_GATE_MIN_FAILURES = 2 is a hardcoded constant in decision_service.py. Operators cannot tune it live — they must open a code file and redeploy. A session with unusual volatility might warrant 3 simultaneou |
| #454 | Confirm the trade decisions cache serves the correct WATCH/AVOID label after the market scan updates mid-session, not the stale one from 10 minutes ago | The /api/trade-decisions route caches its response for 10 minutes (TRADE_DECISIONS_CACHE_MS). If a stock's filter status changes during that window, an operator refreshing within the cache TTL will still see the old WATC |
| #455 | Confirm the gate threshold change takes effect mid-session without restarting the server | Task |
| #456 | Show the active gate threshold on the Trade Decisions page so operators know which value is in effect | An operator can now change high_conf_avoid_gate_min_failures in Settings, but nothing on the Trade Decisions page tells them what value is currently active. If someone changed it to 1 (strict mode), a different operator  |
| #457 | Confirm Performance Analytics and Optimizer tabs show fresh data immediately after a backtest — not after a 60-second wait | Task |
| #458 | Prevent the Overview KPIs from going blank while Performance Analytics and Optimizer data re-fetches after a backtest | When invalidateQueries fires for v2-performance and v2-opt-rec, React Query marks those queries as stale and re-fetches. If the fetch takes a moment, the Overall Win Rate and Best Config Sharpe cards on the Overview tab  |
| #459 | Confirm the LOW RELIABILITY badge also renders correctly in the browser and can't silently disappear | The `low_evidence` badge now has browser-level coverage (Task |
| #460 | Confirm the low-evidence badge still shows after the AI decision cache refreshes mid-session | The current test (Task |
| #461 | Confirm historical_sharpe displays correctly on the Trade Decisions page, not as undefined or zero | The `historical_sharpe` field was missing from the TypeScript `TradeDecision` interface and is now added. However, there are no browser-level tests confirming the value actually renders correctly in the Trade Decisions U |
| #472 | Alert operators immediately when the research pipeline halts so they know paper entries are paused | When `research_failure_mode = fail_closed` and all sources time out, paper entries silently stop. Operators won't notice until they check the Risk Audit page. A push/notification when the mode transitions to PIPELINE_HAL |
| #473 | Confirm the research failure mode setting is reachable from the settings UI so operators can switch modes without touching code | `research_failure_mode` (fail_open / fail_closed) exists in `DEFAULT_SETTINGS` and is validated on update, but there is no UI control for it. Operators who want to tighten or relax the fail-closed behaviour currently nee |
| #474 | Show which research sources are timing out in the ops centre so operators know why the pipeline halted | When all loaders fail, the `research_agent_failure` KV key records the last failure reason but only for the final source. Operators can't tell whether event_intelligence, macro_intelligence, or research_lab is the culpri |
| #475 | Confirm the 'Awaiting first scan' notice disappears once the first scan completes and real pipeline data is present | The cold-start banner in the AI Operations Centre Supervisor card shows "Awaiting first scan" when `pipeline_cold_start=true`. Once a scan completes and pipeline topics publish, the banner should disappear and be replace |
| #476 | Prevent the Supervisor recommendations panel from showing duplicate entries when stale and violation conditions overlap | The supervisor snapshot builds recommendations by appending conditionally. If both STALE_DATA and DEPENDENCY_VIOLATION categories apply to the same topic, the action text can repeat the topic name in two separate cards.  |
| #483 | Fix the 3 failing trailing-stop safety tests so exit protection is provably working | `artifacts/api-server/src/python/test_phase20.py` has 3 pre-existing failures in `TestExitsSafety` (test_no_exit_keeps_position_open, test_trailing_stop_not_armed_without_peak, test_trailing_stop_triggers_after_peak_then |
| #511 | Confirm Investigate links land on the exact moment even when opening an older backtest run | Mission Control event rows and the Mission Timeline now deep-link to /investigation-center?run=&symbol=&trade=&ts=. The page applies params in strict order (run → symbol → trade/ts) to avoid matching against the wrong ru |
| #512 | Verify the Market Session widget shows 'closed' on NSE holidays, not a fake in-session progress bar | MarketSessionWidget (artifacts/trading-dashboard/src/components/mission/SessionWidgets.tsx) computes the session phase from the IST clock and now defers to the authoritative SSE market state (is_open=false → closed). On  |
| #513 | Make Market Breadth show real advance/decline numbers instead of dashes during market hours | MarketBreadthWidget (SessionWidgets.tsx) reads the phase 7.1 market-intelligence endpoints; several spec fields (volume breadth, some sector rows) render "—" because no canonical source exposes them. During a live sessio |
| #514 | Confirm a full certification run finishes fast enough for the dashboard without stacking slow Python processes | POST /api/certification/run executes all six validators plus learning and mission-control checks in one Python process (up to 10 minutes allowed). The data validator fetches candles for up to 10 watchlist symbols and the |
| #515 | Prevent old certification history from growing unbounded in the database | Certification runs persist append-only to the `certification_runs` table (full report JSON per row, potentially large because each domain embeds its full check evidence). If certification is run on a schedule or from the |
| #516 | Make sure a certification READY verdict can't be based on a stale backtest or yesterday's scan | The certification engine validates replay/AI determinism against the LATEST COMPLETED backtest run and mission control against the latest scan snapshot — but it does not check how old they are. An operator could see READ |

---

*States follow the workspace task panel: Drafts are proposals not yet accepted; Active tasks are accepted or already being worked on. Descriptions are the opening "What & Why" line of each task's plan.*
