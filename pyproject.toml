[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "kiteconnect>=5.2.0",
    "numpy>=2.4.6",
    "openpyxl>=3.1",
    "pandas>=3.0.3",
    "psycopg2-binary>=2.9.12",
    "reportlab>=4.0",
    "yfinance>=1.5.1",
    # Database ORM — used by src/database/ repositories (SQLAlchemy async engine + asyncpg driver)
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
]
